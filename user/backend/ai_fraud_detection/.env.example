# Local development only — set these in Render Dashboard for production

# Port for `python app.py` locally (Render uses PORT via gunicorn)
AI_PORT=5001

# development | production
FLASK_ENV=development

# Optional: path to election CSV for baseline anomaly scoring
# CSV_PATH=/path/to/cleaned_dataset_final.csv

# Optional logging level
LOG_LEVEL=INFO
